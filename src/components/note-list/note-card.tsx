import paths from "@/paths"
import Link from "next/link"

// This should be ultimately autogenerated from supabase

type NoteDataType = {
  id: number
  title: string
  content: string
  user_id: number
  notebook_id: number
  created_at: number
  updated_at: number
}

type NoteListProps = {
  data: NoteDataType
}

const NoteCard = ({ data }: NoteListProps) => {
  const { id, title } = data
  return (
    <div>
      <Link href={paths.showNote(title, id)}>{title}</Link>
    </div>
  )
}

export default NoteCard
